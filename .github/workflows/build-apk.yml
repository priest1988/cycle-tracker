name: Build Android APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install buildozer cython==0.29.36

    - name: Build APK
      run: buildozer android debug

    - name: Upload APK
      uses: actions/upload-artifact@v3
      with:
        name: cycle-tracker-apk
        path: bin/*.apk
```

5. `Ctrl+S` - сохранить

---

## Шаг 4: Создайте файл .gitignore

1. **Правой кнопкой** на `pythonProject1`
2. `New` → `File`
3. Назовите: `.gitignore`
4. Вставьте:
```
.buildozer/
bin/
__pycache__/
*.pyc
.idea/
*.log
.DS_Store